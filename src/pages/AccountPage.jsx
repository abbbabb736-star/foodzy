export function AccountPage() {
  return (
    <section className="container page-section">
      <h2 className="section-title">Account</h2>
      <div className="account-card">
        <p>
          This section is prepared for authentication/profile integration. You can connect it to Supabase auth later without changing the routing structure.
        </p>
      </div>
    </section>
  );
}
